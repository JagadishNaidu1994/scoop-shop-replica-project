[build]
  # Build command to run from the root directory
  command = "npm run build"
  # Publish directory where Vite outputs the build artifacts
  publish = "dist"

[[redirects]]
  # Redirect rules for SPA (single page application)
  from = "/*"
  to = "/index.html"
  status = 200
